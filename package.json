{
  "name": "tact-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @tact/web dev",
    "build": "pnpm -r build",
    "preview": "pnpm --filter @tact/web preview",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@total-typescript/ts-reset": "^0.5.1",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.28.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "@types/jszip": "^3.4.1",
    "jszip": "^3.10.1"
  }
}