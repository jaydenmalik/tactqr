# App Configuration
VITE_APP_NAME=Tact
VITE_APP_VERSION=1.0.0

# Offline Storage Configuration
# These are used as keys for localStorage
VITE_STORAGE_PREFIX=tact_

# Export/Import Configuration
VITE_EXPORT_VERSION=1.0.0
VITE_MAX_EXPORT_SIZE=50000  # Maximum size in bytes for QR code
VITE_COMPRESSION_LEVEL=6    # Gzip compression level (1-9)

# Security Configuration
VITE_CRYPTO_ITERATIONS=100000  # PBKDF2 iterations for key derivation 